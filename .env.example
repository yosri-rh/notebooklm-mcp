# NotebookLM MCP Server - Local Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# Browser Configuration
# ============================================================================

# Run browser in headless mode (no GUI)
# Set to "false" to see the browser for debugging
NOTEBOOKLM_HEADLESS=true

# ============================================================================
# Logging Configuration
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================================================
# MCP Transport Configuration (for container deployments)
# ============================================================================

# Transport mode: "stdio" (default for Claude Desktop) or "streamable-http" (for containers)
# MCP_TRANSPORT=stdio

# HTTP server bind address (only used with streamable-http)
# MCP_HOST=0.0.0.0

# HTTP server port (only used with streamable-http)
# MCP_PORT=8080

# ============================================================================
# Notes
# ============================================================================

# For local Claude Desktop usage:
#   - Keep MCP_TRANSPORT commented out (defaults to stdio)
#   - Set NOTEBOOKLM_HEADLESS=true for background operation
#   - Use LOG_LEVEL=DEBUG if troubleshooting issues

# For local Podman/Docker testing:
#   - Same as above for stdio mode
#   - Or set MCP_TRANSPORT=streamable-http for HTTP testing

# For Kubernetes/OpenShift (experimental):
#   - Set MCP_TRANSPORT=streamable-http
#   - Set MCP_HOST=0.0.0.0
#   - Set MCP_PORT=8080
